<div id="splashplanation">
  <h1 id="splash-title">DBC Overflow</h1>
  <div id="call-to-action">
    <div id="splash-options">
      <% if session[:user_id] %>
        <p>Welcome, <a href="/users/<%= user.id %>" class="nav-link"><%= user.username %></a>!</p>
        <p>We missed you so, so much.</p>
        <p>You've currently asked <%= user.questions_asked %> questions.
      <% else %>
        <%= render partial: "layouts/splashlog" %>
      <% end %>
    </ul>
  </div>
</div>
